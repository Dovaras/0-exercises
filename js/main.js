const allIngredients = document.querySelectorAll('li');

//allIngredients.forEach(element => console.log(element));

allIngredients.toUpperCase();

//allIngredients.forEach(element => console.log(element));

for (const i = 0; i < allIngredients.length; i++){
    console.log(i);
}